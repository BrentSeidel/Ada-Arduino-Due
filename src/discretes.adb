with BBS.embed.Due.GPIO;
package body discretes is
   procedure init is
   begin
      pin(0) := BBS.embed.Due.GPIO.pin0;
      pin(1) := BBS.embed.Due.GPIO.pin1;
      pin(2) := BBS.embed.Due.GPIO.pin2;
      pin(3) := BBS.embed.Due.GPIO.pin3;
      pin(4) := BBS.embed.Due.GPIO.pin4;  -- Don't use this one
      pin(5) := BBS.embed.Due.GPIO.pin5;
      pin(6) := BBS.embed.Due.GPIO.pin6;
      pin(7) := BBS.embed.Due.GPIO.pin7;
      pin(8) := BBS.embed.Due.GPIO.pin8;
      pin(9) := BBS.embed.Due.GPIO.pin9;

      pin(10) := BBS.embed.Due.GPIO.pin10;
      pin(11) := BBS.embed.Due.GPIO.pin11;
      pin(12) := BBS.embed.Due.GPIO.pin12;
      pin(13) := BBS.embed.Due.GPIO.pin13;
      pin(14) := BBS.embed.Due.GPIO.pin14;
      pin(15) := BBS.embed.Due.GPIO.pin15;
      pin(16) := BBS.embed.Due.GPIO.pin16;
      pin(17) := BBS.embed.Due.GPIO.pin17;
      pin(18) := BBS.embed.Due.GPIO.pin18;
      pin(19) := BBS.embed.Due.GPIO.pin19;

      pin(20) := BBS.embed.Due.GPIO.pin20;
      pin(21) := BBS.embed.Due.GPIO.pin21;
      pin(22) := BBS.embed.Due.GPIO.pin22;
      pin(23) := BBS.embed.Due.GPIO.pin23;
      pin(24) := BBS.embed.Due.GPIO.pin24;
      pin(25) := BBS.embed.Due.GPIO.pin25;
      pin(26) := BBS.embed.Due.GPIO.pin26;
      pin(27) := BBS.embed.Due.GPIO.pin27;
      pin(28) := BBS.embed.Due.GPIO.pin28;
      pin(29) := BBS.embed.Due.GPIO.pin29;

      pin(30) := BBS.embed.Due.GPIO.pin30;
      pin(31) := BBS.embed.Due.GPIO.pin31;
      pin(32) := BBS.embed.Due.GPIO.pin32;
      pin(33) := BBS.embed.Due.GPIO.pin33;
      pin(34) := BBS.embed.Due.GPIO.pin34;
      pin(35) := BBS.embed.Due.GPIO.pin35;
      pin(36) := BBS.embed.Due.GPIO.pin36;
      pin(37) := BBS.embed.Due.GPIO.pin37;
      pin(38) := BBS.embed.Due.GPIO.pin38;
      pin(39) := BBS.embed.Due.GPIO.pin39;

      pin(40) := BBS.embed.Due.GPIO.pin40;
      pin(41) := BBS.embed.Due.GPIO.pin41;
      pin(42) := BBS.embed.Due.GPIO.pin42;
      pin(43) := BBS.embed.Due.GPIO.pin43;
      pin(44) := BBS.embed.Due.GPIO.pin44;
      pin(45) := BBS.embed.Due.GPIO.pin45;
      pin(46) := BBS.embed.Due.GPIO.pin46;
      pin(47) := BBS.embed.Due.GPIO.pin47;
      pin(48) := BBS.embed.Due.GPIO.pin48;
      pin(49) := BBS.embed.Due.GPIO.pin49;

      pin(50) := BBS.embed.Due.GPIO.pin50;
      pin(51) := BBS.embed.Due.GPIO.pin51;
      pin(52) := BBS.embed.Due.GPIO.pin52;
      pin(53) := BBS.embed.Due.GPIO.pin53;
   end;

end;
